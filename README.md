*This project has been created as part of the 42 curriculum by mohamed.*

# Inception (Docker Compose project)

Description: Small infrastructure using Docker Compose: `nginx` (TLS), `wordpress`+`php-fpm`, and `mariadb`.

See `USER_DOC.md` and `DEV_DOC.md` for usage and developer instructions.
